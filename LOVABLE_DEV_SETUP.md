# Запуск Telegram бота на lovable.dev

## Обзор

Lovable.dev - это платформа для разработки и развертывания Telegram-ботов. Ниже приведены инструкции по запуску вашего Telegram бота с поддержкой реферальных ссылок на этой платформе.

## Подготовка файлов

Для запуска бота на lovable.dev вам понадобятся следующие файлы:

### 1. bot_setup.py

Вот обновленная версия файла с корректной обработкой команды `/start` и inline-кнопкой:

```python
import asyncio
from aiogram import Bot, Dispatcher
from aiogram.types import Message
from aiogram.filters import CommandStart
from aiogram.fsm.storage.memory import MemoryStorage
from aiogram.enums import ParseMode
from aiogram.client.default import DefaultBotProperties
import os
import logging

# Включаем логирование
logging.basicConfig(level=logging.INFO)

# Получаем токен бота из переменных окружения
BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

# URL для WebApp (обновите на ваш домен)
WEBAPP_URL = os.getenv("WEBAPP_URL", "https://your-app-domain.com")

# Создаем бота и диспетчер
from aiogram.client.default import DefaultBotProperties
bot = Bot(token=BOT_TOKEN, default=DefaultBotProperties(parse_mode=ParseMode.HTML))
storage = MemoryStorage()
dp = Dispatcher(storage=storage)

@dp.message(CommandStart())
async def command_start_handler(message: Message) -> None:
    """
    Обработчик команды /start с параметрами
    """
    # Получаем параметр из команды /start
    start_param = message.text.split()[1] if len(message.text.split()) > 1 else None
    
    user_id = message.from_user.id
    user_name = message.from_user.full_name
    
    # Если есть реферальный параметр
    if start_param:
        # Логируем реферальный переход
        print(f"Пользователь {user_name} (ID: {user_id}) перешел по реферальной ссылке с параметром: {start_param}")
        
        # Отправляем приветственное сообщение с информацией о переходе в приложение
        welcome_text = f"""
Привет, {user_name}! 👋

Вы перешли по реферальной ссылке и будете перенаправлены в приложение.
Нажмите кнопку ниже для перехода:
        """
        
        # Отправляем сообщение с WebApp
        await message.answer(
            welcome_text,
            reply_markup=get_webapp_keyboard(start_param)
        )
    else:
        # Если нет реферального параметра
        welcome_text = f"""
Привет, {user_name}! 👋

Добро пожаловать в наше приложение! 
Для начала работы нажмите кнопку ниже:
        """
        
        await message.answer(
            welcome_text,
            reply_markup=get_webapp_keyboard()
        )

def get_webapp_keyboard(start_param=None):
    """
    Создает клавиатуру с кнопкой для открытия WebApp
    """
    from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
    
    # Формируем URL для WebApp с параметром start
    webapp_url = WEBAPP_URL
    if start_param:
        webapp_url = f"{WEBAPP_URL}?startapp={start_param}"
    else:
        webapp_url = f"{WEBAPP_URL}"
    
    keyboard = InlineKeyboardMarkup(
        inline_keyboard=[
            [
                InlineKeyboardButton(
                    text="📱 Открыть приложение",
                    web_app={"url": webapp_url}
                )
            ]
        ]
    )
    
    return keyboard

@dp.message()
async def handle_message(message: Message) -> None:
    """
    Обработчик всех остальных сообщений
    """
    # Для всех других сообщений
    await message.answer(
        "Нажмите кнопку ниже, чтобы открыть приложение:",
        reply_markup=get_webapp_keyboard()
    )

async def main():
    """
    Основная функция запуска бота
    """
    # Запускаем бота
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
```

### 2. requirements.txt

```
aiogram==3.16.0
python-dotenv==1.0.1
```

## Настройка на lovable.dev

### Шаг 1: Создание проекта

1. Зайдите на [lovable.dev](https://lovable.dev)
2. Нажмите "Create New Bot"
3. Выберите "Python" в качестве языка программирования
4. Назовите ваш бот (например, "Referral Bot")

### Шаг 2: Загрузка файлов

1. В редакторе кода на lovable.dev создайте файл `bot.py` и вставьте в него содержимое из `bot_setup.py`
2. Создайте файл `requirements.txt` и добавьте в него зависимости

### Шаг 3: Настройка переменных окружения

1. Перейдите в раздел "Environment Variables" или "Config Variables"
2. Добавьте следующие переменные:
   - `TELEGRAM_BOT_TOKEN`: ваш токен бота от @BotFather
   - `WEBAPP_URL`: URL вашего WebApp (например, https://your-app-domain.lovable.dev или ваш домен)

### Шаг 4: Запуск бота

1. Нажмите кнопку "Run" или "Start" на панели управления
2. Убедитесь, что бот запущен и не имеет ошибок в логах
3. Проверьте, что бот отвечает на команду `/start`

## Важные замечания

### WebApp URL

Если вы используете lovable.dev для размещения как бота, так и WebApp, ваш URL может выглядеть как `https://your-username.lovable.dev/bot-name`. Убедитесь, что вы указали правильный URL для WebApp в переменной `WEBAPP_URL`.

### Telegram Bot Token

Получите токен для вашего бота у [@BotFather](https://t.me/BotFather) в Telegram:
1. Отправьте `/newbot` и следуйте инструкциям
2. Выберите имя для бота
3. Выберите username для бота (должен заканчиваться на "bot")
4. Скопируйте токен и добавьте его в переменные окружения

### Web App Integration

Для корректной работы с WebApp, убедитесь, что:

1. Ваш WebApp поддерживает Telegram WebApp SDK
2. В настройках бота в Telegram указан корректный URL для WebApp
3. Используется HTTPS (lovable.dev предоставляет HTTPS по умолчанию)

## Тестирование

После запуска бота:

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Проверьте, что бот отвечает приветственным сообщением
4. Проверьте, что inline-кнопка "📱 Открыть приложение" работает корректно
5. Попробуйте отправить команду `/start` с параметром (например, `/start test`) и убедитесь, что бот корректно обрабатывает реферальные параметры

## Устранение неполадок

### Бот не отвечает

- Проверьте, запущен ли бот на lovable.dev
- Убедитесь, что переменная `TELEGRAM_BOT_TOKEN` указана правильно
- Проверьте логи на lovable.dev на наличие ошибок

### Inline-кнопка не работает

- Убедитесь, что URL в `WEBAPP_URL` указан правильно
- Проверьте, что WebApp доступен по указанному URL
- Убедитесь, что WebApp поддерживает Telegram WebApp SDK

### Ошибки при запуске

- Проверьте, что все зависимости из `requirements.txt` указаны правильно
- Убедитесь, что код не содержит синтаксических ошибок
- Проверьте, что используете версию Python, поддерживаемую lovable.dev