FROM node:18-alpine

WORKDIR /app

# Копируем package файлы
COPY package*.json ./

# Устанавливаем зависимости
RUN npm ci --only=production

# Копируем весь код
COPY . .

# Даем права на выполнение
RUN chmod +x server.cjs app.cjs || true

EXPOSE 3000

# Запускаем сервер
CMD ["node", "server.cjs"]
