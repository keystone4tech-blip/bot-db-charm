# ⚙️ НАСТРОЙКА GITHUB ACTIONS ДЛЯ АВТОМАТИЧЕСКОГО ДЕПЛОЯ

## Пошаговая инструкция

---

## ШАГ 1: Генерация SSH ключей

### На вашем локальном компьютере (Windows/Mac/Linux):

```bash
# Создаем SSH ключ специально для деплоя
ssh-keygen -t rsa -b 4096 -f ~/.ssh/deploy_key -N ""
```

Это создаст два файла:
- `~/.ssh/deploy_key` - **PRIVATE** ключ (никогда не публикуйте!)
- `~/.ssh/deploy_key.pub` - **PUBLIC** ключ (можно делиться)

---

## ШАГ 2: Копирование PUBLIC ключа

### Скопируйте PUBLIC ключ:

**Linux/Mac:**
```bash
cat ~/.ssh/deploy_key.pub
```

**Windows (PowerShell):**
```powershell
type ~\.ssh\deploy_key.pub
```

**Или используйте графический редактор:**
```bash
# Linux/Mac
nano ~/.ssh/deploy_key.pub

# Windows
notepad %USERPROFILE%\.ssh\deploy_key.pub
```

Скопируйте ВЕСЬ текст (от `ssh-rsa` до конца).

---

## ШАГ 3: Добавление PUBLIC ключа на сервер

### Подключитесь к серверу:

```bash
ssh root@your-server-ip
# или
ssh your-username@your-server-ip
```

### Добавьте PUBLIC ключ в authorized_keys:

```bash
# Создайте директорию .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Откройте файл authorized_keys
nano ~/.ssh/authorized_keys

# Вставьте ваш PUBLIC ключ в конец файла
# (Каждый ключ на новой строке)

# Сохраните: Ctrl+X, затем Y, затем Enter

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys
```

**Или используйте одну команду:**

```bash
echo "ваш_public_ключ_здесь" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

### Проверьте подключение:

На локальном компьютере попробуйте подключиться с новым ключом:

```bash
ssh -i ~/.ssh/deploy_key root@your-server-ip
```

Если подключение успешно - ключ настроен правильно! ✅

---

## ШАГ 4: Копирование PRIVATE ключа

### Скопируйте PRIVATE ключ:

**Linux/Mac:**
```bash
cat ~/.ssh/deploy_key
```

**Windows (PowerShell):**
```powershell
type ~\.ssh\deploy_key
```

Скопируйте ВЕСЬ текст, включая строки:
```
-----BEGIN OPENSSH PRIVATE KEY-----
...
-----END OPENSSH PRIVATE KEY-----
```

---

## ШАГ 5: Добавление Secrets в GitHub

### 1. Откройте ваш репозиторий на GitHub

### 2. Перейдите в Settings

Нажмите на вкладку **Settings** в верхнем меню репозитория.

### 3. Откройте Secrets and Variables

В левом меню выберите:
**Secrets and variables → Actions**

### 4. Добавьте Secrets

Нажмите кнопку **New repository secret** и добавьте следующие секреты:

#### SECRET 1: SERVER_IP
- **Name:** `SERVER_IP`
- **Value:** IP адрес вашего сервера (например: `123.45.67.89`)
- Нажмите **Add secret**

#### SECRET 2: SERVER_USER
- **Name:** `SERVER_USER`
- **Value:** Имя пользователя на сервере (например: `root` или `ubuntu`)
- Нажмите **Add secret**

#### SECRET 3: SERVER_SSH_KEY
- **Name:** `SERVER_SSH_KEY`
- **Value:** ВЕСЬ текст PRIVATE ключа из файла `~/.ssh/deploy_key`
- **ВАЖНО:** Скопируйте всё содержимое, включая заголовки `-----BEGIN...` и `-----END...`
- Нажмите **Add secret**

#### SECRET 4: SERVER_PORT (опционально)
- **Name:** `SERVER_PORT`
- **Value:** `22` (или другой порт SSH, если вы его изменили)
- Нажмите **Add secret**

---

## ШАГ 6: Проверка workflow файла

### Убедитесь, что файл `.github/workflows/deploy.yml` существует:

```bash
cat .github/workflows/deploy.yml
```

Файл уже должен быть создан в вашем проекте.

---

## ШАГ 7: Тестирование автоматического деплоя

### 1. Внесите изменения в код

```bash
# На локальном компьютере
cd /путь/к/вашему/проекту

# Создайте тестовый файл или измените README
echo "# Test deploy $(date)" >> README.md

# Добавьте изменения
git add .

# Сделайте коммит
git commit -m "Test: automatic deploy"

# Отправьте в GitHub
git push origin main
```

### 2. Проверьте GitHub Actions

1. Откройте ваш репозиторий на GitHub
2. Перейдите на вкладку **Actions**
3. Вы должны увидеть запущенный workflow: **"🚀 Автоматический деплой на сервер"**

### 3. Следите за процессом

Кликните на запущенный workflow, чтобы увидеть логи в реальном времени.

Workflow выполнит:
- ✅ Подключение к серверу через SSH
- ✅ Переход в директорию проекта
- ✅ Загрузку последних изменений из GitHub
- ✅ Остановку старых контейнеров
- ✅ Сборку новых Docker образов
- ✅ Запуск обновленных контейнеров
- ✅ Показ логов

### 4. Проверьте на сервере

Подключитесь к серверу и проверьте:

```bash
ssh root@your-server-ip
cd ~/keystone_project

# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs --tail=50
```

---

## ПРОВЕРОЧНЫЙ ЧЕК-ЛИСТ

Перед первым деплоем убедитесь:

### На локальном компьютере:
- [ ] SSH ключ создан (`~/.ssh/deploy_key`)
- [ ] PUBLIC ключ скопирован
- [ ] PRIVATE ключ скопирован

### На сервере:
- [ ] PUBLIC ключ добавлен в `~/.ssh/authorized_keys`
- [ ] Права установлены (600 на authorized_keys, 700 на .ssh)
- [ ] Тестовое подключение с ключом работает
- [ ] Проект развернут в `~/keystone_project`
- [ ] Docker и Docker Compose установлены

### В GitHub:
- [ ] Секрет `SERVER_IP` добавлен
- [ ] Секрет `SERVER_USER` добавлен
- [ ] Секрет `SERVER_SSH_KEY` добавлен (весь PRIVATE ключ!)
- [ ] Секрет `SERVER_PORT` добавлен (опционально)
- [ ] Файл `.github/workflows/deploy.yml` существует

### Тестирование:
- [ ] Тестовый коммит сделан
- [ ] GitHub Actions workflow запустился
- [ ] Workflow завершился успешно (зеленая галочка)
- [ ] Изменения появились на сервере
- [ ] Контейнеры перезапустились
- [ ] Бот/Backend работают

---

## РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "Permission denied (publickey)"

**Проблема:** GitHub Actions не может подключиться к серверу.

**Решение:**
1. Проверьте, что PUBLIC ключ добавлен в `~/.ssh/authorized_keys` на сервере
2. Проверьте права на файлы:
   ```bash
   chmod 700 ~/.ssh
   chmod 600 ~/.ssh/authorized_keys
   ```
3. Проверьте, что PRIVATE ключ полностью скопирован в `SERVER_SSH_KEY`

### Ошибка: "Host key verification failed"

**Проблема:** Сервер не в списке известных хостов.

**Решение:** Обновите workflow файл, добавив:
```yaml
script_stop: false
```

Или подключитесь к серверу вручную один раз:
```bash
ssh -i ~/.ssh/deploy_key root@your-server-ip
# Ответьте "yes" на вопрос о добавлении в known_hosts
```

### Ошибка: "Directory not found"

**Проблема:** Директория `~/keystone_project` не существует на сервере.

**Решение:** Запустите setup скрипт на сервере:
```bash
./deploy/setup.sh
```

### Ошибка: "docker-compose: command not found"

**Проблема:** Docker Compose не установлен на сервере.

**Решение:** Установите Docker Compose:
```bash
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

---

## ДОПОЛНИТЕЛЬНАЯ БЕЗОПАСНОСТЬ

### 1. Используйте ограниченного пользователя

Вместо `root` создайте отдельного пользователя для деплоя:

```bash
# На сервере (под root)
adduser deploy
usermod -aG docker deploy
usermod -aG sudo deploy

# Скопируйте authorized_keys
mkdir -p /home/deploy/.ssh
cp ~/.ssh/authorized_keys /home/deploy/.ssh/
chown -R deploy:deploy /home/deploy/.ssh
chmod 700 /home/deploy/.ssh
chmod 600 /home/deploy/.ssh/authorized_keys

# В GitHub Secrets измените SERVER_USER на "deploy"
```

### 2. Ограничьте права SSH ключа

Добавьте ограничения в `authorized_keys`:

```bash
# На сервере
nano ~/.ssh/authorized_keys

# Добавьте перед ключом:
command="cd ~/keystone_project && docker-compose up -d --build",no-port-forwarding,no-X11-forwarding,no-agent-forwarding ssh-rsa AAAA...
```

### 3. Используйте отдельную ветку для деплоя

Создайте ветку `production`:

```bash
git checkout -b production
git push origin production
```

Измените в `.github/workflows/deploy.yml`:
```yaml
on:
  push:
    branches: 
      - production
```

---

## ПОЛЕЗНЫЕ КОМАНДЫ

### Локально (ваш компьютер):

```bash
# Проверить SSH ключ
ls -la ~/.ssh/deploy_key*

# Посмотреть PUBLIC ключ
cat ~/.ssh/deploy_key.pub

# Посмотреть PRIVATE ключ
cat ~/.ssh/deploy_key

# Тестовое подключение
ssh -i ~/.ssh/deploy_key root@your-server-ip

# Сделать коммит и пуш (запустит деплой)
git add .
git commit -m "Update"
git push origin main
```

### На сервере:

```bash
# Проверить authorized_keys
cat ~/.ssh/authorized_keys

# Проверить логи SSH
sudo tail -f /var/log/auth.log

# Проверить статус проекта
cd ~/keystone_project
docker-compose ps
docker-compose logs -f
```

### GitHub:

- Посмотреть логи деплоя: **Actions → выбрать workflow**
- Перезапустить деплой: **Actions → выбрать workflow → Re-run jobs**

---

**Готово! Теперь каждый `git push` автоматически обновляет ваш проект на сервере! 🚀**
