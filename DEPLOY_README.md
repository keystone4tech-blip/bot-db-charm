# 🚀 АВТОМАТИЗАЦИЯ ДЕПЛОЯ - ГЛАВНАЯ ИНСТРУКЦИЯ

## 📚 Навигация по документации

Выберите нужную инструкцию в зависимости от вашей задачи:

---

## 🎯 Быстрый старт

### Вы хотите: Развернуть проект за 5 минут

**→ Читайте:** [QUICK_DEPLOY.md](./QUICK_DEPLOY.md)

Краткая инструкция для тех, кто хочет быстро развернуть проект на сервере.

---

## 📖 Полное руководство

### Вы хотите: Понять весь процесс и настроить всё правильно

**→ Читайте:** [DEPLOY_FULL_GUIDE.md](./DEPLOY_FULL_GUIDE.md)

Подробная инструкция с объяснениями каждого шага:
- Установка всех компонентов
- Настройка домена и SSL
- Конфигурация Nginx
- Безопасность
- Мониторинг
- Решение проблем

---

## ⚙️ Настройка GitHub Actions

### Вы хотите: Настроить автоматический деплой при git push

**→ Читайте:** [GITHUB_ACTIONS_SETUP.md](./GITHUB_ACTIONS_SETUP.md)

Пошаговая инструкция:
- Генерация SSH ключей
- Добавление ключей на сервер
- Настройка GitHub Secrets
- Тестирование автоматического деплоя

---

## ❓ Часто задаваемые вопросы

### Вы хотите: Найти решение конкретной проблемы

**→ Читайте:** [DEPLOY_FAQ.md](./DEPLOY_FAQ.md)

Ответы на частые вопросы:
- Docker проблемы
- База данных
- GitHub Actions
- Nginx и SSL
- Telegram Bot
- Безопасность
- Производительность

---

## 🛠️ Скрипты

Все скрипты находятся в папке `deploy/`:

### setup.sh
**Главный установочный скрипт**

Устанавливает всё необходимое на чистый Ubuntu сервер.

```bash
chmod +x deploy/setup.sh
./deploy/setup.sh
```

### quick-update.sh
**Быстрое обновление проекта**

Обновляет код из GitHub и перезапускает контейнеры.

```bash
./deploy/quick-update.sh
```

### generate-secrets.sh
**Генерация безопасных секретов**

Генерирует случайные значения для паролей и ключей.

```bash
./deploy/generate-secrets.sh
```

---

## 📦 Что входит в автоматизацию?

### ✅ Один setup скрипт устанавливает:
- Docker & Docker Compose
- Node.js 18
- Python 3.11
- PostgreSQL 15
- Nginx
- Certbot (Let's Encrypt)

### ✅ Автоматический деплой через GitHub Actions:
- Push в GitHub → автоматическое обновление на сервере
- Пересборка Docker контейнеров
- Перезапуск всех сервисов
- Показ логов

### ✅ Готовая конфигурация:
- Docker Compose для всех сервисов
- Nginx с SSL сертификатом
- Автоматическое обновление SSL
- Переменные окружения

---

## 🎯 Что вам нужно подготовить?

### Обязательно:
- ✅ Ubuntu сервер (20.04+)
- ✅ Root доступ или sudo права
- ✅ GitHub репозиторий

### Опционально (но рекомендуется):
- 🔹 Домен (для SSL и красивого адреса)
- 🔹 SSH ключ (для GitHub Actions)

---

## 🚀 Быстрый план действий

### Шаг 1: Подготовка
1. Получите доступ к Ubuntu серверу
2. Клонируйте репозиторий (или он будет клонирован автоматически)

### Шаг 2: Установка
```bash
# На сервере
./deploy/setup.sh
```

### Шаг 3: Настройка
```bash
# Отредактируйте .env
nano ~/keystone_project/.env

# Перезапустите
docker-compose down && docker-compose up -d
```

### Шаг 4: GitHub Actions (опционально)
1. Создайте SSH ключ
2. Добавьте PUBLIC ключ на сервер
3. Добавьте PRIVATE ключ в GitHub Secrets
4. Сделайте git push - деплой произойдет автоматически!

---

## 📊 Структура проекта после установки

```
/home/user/keystone_project/
├── .github/
│   └── workflows/
│       └── deploy.yml          # ← GitHub Actions workflow
├── deploy/
│   ├── setup.sh               # ← Главный setup скрипт
│   ├── quick-update.sh        # ← Быстрое обновление
│   └── generate-secrets.sh    # ← Генерация секретов
├── telegram_bot/              # Код бота
├── config/                    # Backend конфиг
├── controllers/               # Backend контроллеры
├── routes/                    # Backend маршруты
├── services/                  # Backend сервисы
├── .env                       # ← Ваши настройки
├── .env.example               # Пример настроек
├── docker-compose.yml         # ← Docker конфигурация
├── Dockerfile.backend         # Backend образ
├── Dockerfile.bot             # Bot образ
└── ...
```

---

## 🔍 Проверка работы

### Backend
```bash
curl http://localhost:3000/api/users
# Или с доменом
curl https://yourdomain.com/api/users
```

### Telegram Bot
Отправьте `/start` вашему боту в Telegram

### Docker
```bash
docker-compose ps
docker-compose logs -f
```

### База данных
```bash
docker-compose exec postgres psql -U postgres -d keystone
```

---

## 🆘 Помощь и поддержка

### Если что-то не работает:

1. **Проверьте логи:**
   ```bash
   docker-compose logs -f
   ```

2. **Проверьте статус:**
   ```bash
   docker-compose ps
   ```

3. **Перезапустите:**
   ```bash
   docker-compose restart
   ```

4. **Пересоберите:**
   ```bash
   docker-compose up -d --build
   ```

5. **Проверьте FAQ:**
   См. [DEPLOY_FAQ.md](./DEPLOY_FAQ.md)

---

## 📞 Полезные команды

### Docker
```bash
docker-compose ps              # Статус
docker-compose logs -f         # Все логи
docker-compose logs -f bot     # Логи бота
docker-compose restart         # Перезапуск
docker-compose down            # Остановить
docker-compose up -d --build   # Пересобрать и запустить
```

### Git
```bash
git pull origin main           # Обновить код
git status                     # Статус
git log --oneline -10          # Последние коммиты
```

### Nginx
```bash
sudo nginx -t                  # Проверка конфига
sudo systemctl reload nginx    # Перезагрузка
sudo tail -f /var/log/nginx/error.log  # Логи
```

### SSL
```bash
sudo certbot certificates      # Статус сертификатов
sudo certbot renew             # Обновить
```

---

## 🎉 Результат

После выполнения всех шагов у вас будет:

✅ **Работающий сервер** с Docker, Node.js, Python, PostgreSQL  
✅ **Автоматический деплой** - git push → обновление на сервере  
✅ **SSL сертификат** - HTTPS работает  
✅ **Домен привязан** - красивый адрес вместо IP  
✅ **Мониторинг** - логи и статус всех сервисов  
✅ **Безопасность** - firewall, секреты, ключи  

---

## 📝 Следующие шаги

После успешного деплоя:

1. ✅ Настройте резервное копирование базы данных
2. ✅ Настройте мониторинг (Portainer, Grafana)
3. ✅ Настройте алерты об ошибках (Sentry)
4. ✅ Настройте логирование (ELK stack)
5. ✅ Проверьте безопасность (fail2ban, rate limiting)

---

## 🔗 Быстрые ссылки

- **Быстрый старт:** [QUICK_DEPLOY.md](./QUICK_DEPLOY.md)
- **Полное руководство:** [DEPLOY_FULL_GUIDE.md](./DEPLOY_FULL_GUIDE.md)
- **GitHub Actions:** [GITHUB_ACTIONS_SETUP.md](./GITHUB_ACTIONS_SETUP.md)
- **FAQ:** [DEPLOY_FAQ.md](./DEPLOY_FAQ.md)
- **Скрипты:** [deploy/README.md](./deploy/README.md)

---

**Готовы начать? Выберите инструкцию выше и вперед! 🚀**
