# Keystone Mobile App

Полнофункциональное мобильное приложение для Android и iOS на базе React Native и Expo.

## 📱 Возможности

- 🔐 Аутентификация через Telegram ID
- 🔑 Управление VPN ключами
- 💬 Управление Telegram каналами
- 👥 Реферальная программа
- 🔔 Push уведомления через Firebase
- 🔄 OTA обновления через Expo Updates
- 📊 Админ панель

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- npm или yarn
- Android Studio (для Android)
- Xcode (для iOS, только macOS)

### Установка

```bash
# Установить зависимости
npm install

# Запустить Expo
npm start
```

### Запуск на эмуляторе

```bash
# Android
npm run android

# iOS
npm run ios

# Web
npm run web
```

## ⚙️ Настройка

### 1. Переменные окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Обязательные переменные:
- `EXPO_PUBLIC_API_BASE_URL` - URL вашего backend API
- `EXPO_PUBLIC_FIREBASE_API_KEY` - Firebase API ключ
- `EXPO_PUBLIC_FIREBASE_PROJECT_ID` - Firebase Project ID
- `EXPO_PUBLIC_FIREBASE_APP_ID` - Firebase App ID

### 2. Firebase настройка

1. Создайте проект в [Firebase Console](https://console.firebase.google.com/)
2. Включите Cloud Messaging
3. Добавьте приложения Android и iOS
4. Скачайте конфигурационные файлы:
   - `google-services.json` для Android
   - `GoogleService-Info.plist` для iOS
5. Поместите файлы в папку `firebase/`

### 3. EAS настройка

1. Установите EAS CLI:
```bash
npm install -g eas-cli
```

2. Войдите в Expo:
```bash
eas login
```

3. Настройте проект:
```bash
eas build:configure
```

## 📦 Сборка приложения

### APK (для установки вручную)

```bash
npm run build:apk
```

### AAB (для Google Play)

```bash
npm run build:aab
```

### iOS (для App Store)

```bash
npm run build:ios
```

## 📁 Структура проекта

```
mobile-app/
├── src/
│   ├── screens/        # Экраны приложения
│   ├── components/     # Переиспользуемые компоненты
│   ├── services/       # Сервисы (API, Firebase, etc)
│   ├── navigation/     # Навигация
│   ├── redux/          # Redux store
│   ├── hooks/          # Custom hooks
│   ├── types/          # TypeScript типы
│   ├── utils/          # Утилиты и хелперы
│   └── styles/         # Стили и тема
├── firebase/           # Firebase конфигурация
├── android/            # Android нативный код
├── ios/                # iOS нативный код
└── ...
```

## 🔧 Доступные скрипты

```bash
npm start           # Запустить development сервер
npm run android     # Запустить на Android
npm run ios         # Запустить на iOS
npm run web         # Запустить в браузере
npm run lint        # Запустить линтер
npm run build:apk   # Собрать APK
npm run build:aab   # Собрать AAB
```

## 📝 Backend API

Приложение подключается к backend API по следующим эндпоинтам:

- `POST /api/users/register` - Регистрация пользователя
- `GET /api/users/:telegramId` - Получить пользователя
- `POST /api/referral-code/verify` - Верифицировать реф. код
- `GET /api/vpn-keys/:userId` - Получить VPN ключи
- `POST /api/vpn-keys` - Создать VPN ключ
- `GET /api/telegram-channels/:userId` - Получить каналы
- `POST /api/telegram-channels` - Добавить канал
- `GET /api/referral-stats/:userId` - Статистика рефералов

## 🔐 Аутентификация

Приложение использует Telegram ID для аутентификации. Пользователь должен:
1. Зарегистрироваться через Telegram бота
2. Получить свой Telegram ID от бота
3. Войти в мобильное приложение используя Telegram ID

## 📲 Push уведомления

Push уведомления реализованы через Firebase Cloud Messaging:

- Уведомления о новых рефералах
- Уведомления о действиях с VPN ключами
- Системные уведомления

## 🔄 OTA обновления

Приложение поддерживает OTA (Over-The-Air) обновления через Expo Updates:

- Автоматическая проверка обновлений при запуске
- Скачивание и установка обновлений без App Store/Google Play
- Graceful reload с сохранением состояния

## 🐛 Устранение проблем

### Приложение не запускается

```bash
# Очистите кэш
expo r -c

# Переустановите node_modules
rm -rf node_modules
npm install
```

### Push уведомления не работают

Проверьте:
1. Firebase конфигурация
2. Разрешения на уведомления в настройках устройства
3. Device token отправлен на backend

### OTA обновления не устанавливаются

Проверьте:
1. EAS проект настроен правильно
2. `runtimeVersion` соответствует версии сборки
3. URL обновлений в `app.json` корректный

## 📄 Лицензия

MIT License
