# 🚀 ИНСТРУКЦИЯ ПО СБОРКЕ APK - KEYSTONE APP

## ✅ Firebase конфигурация УЖЕ ГОТОВА!

Firebase успешно настроен:
- **Project ID**: `keystone-app-95683`
- **Package Name**: `com.keystone_app`
- **API Key**: `AIzaSyCdcKrKTK2XWUWm9mhwLCr_nf64Z39lny8`

Файл `google-services.json` уже создан в папке `firebase/`.

---

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

### 1. Установленное ПО

- **Node.js** 18+ ([скачать](https://nodejs.org/))
- **npm** или **yarn**
- **Git**

### 2. Учётная запись Expo

Создайте аккаунт на [expo.dev](https://expo.dev) если его нет.

---

## 🛠️ ЭТАП 1: ПОДГОТОВКА ПРОЕКТА

### Шаг 1.1: Перейти в папку mobile-app

```bash
cd mobile-app
```

### Шаг 1.2: Установить зависимости

```bash
npm install
```

### Шаг 1.3: Установить EAS CLI глобально

```bash
npm install -g eas-cli
```

---

## 🔑 ЭТАП 2: НАСТРОЙКА EAS

### Шаг 2.1: Войти в Expo аккаунт

```bash
eas login
```

Введите email и пароль от вашего Expo аккаунта.

### Шаг 2.2: Инициализировать EAS проект

```bash
eas build:configure
```

Эта команда:
- Создаст EAS Project ID
- Обновит `app.json` с этим ID
- Настроит проект для сборки

### Шаг 2.3: Проверить app.json

Откройте `app.json` и убедитесь, что есть:

```json
{
  "extra": {
    "eas": {
      "projectId": "ваш-реальный-id-здесь"
    }
  }
}
```

---

## 🔥 ЭТАП 3: НАСТРОЙКА ОКРУЖЕНИЯ

### Шаг 3.1: Обновить .env файл

Откройте `.env` и замените `https://your-domain.com/api` на ваш реальный API URL:

```env
EXPO_PUBLIC_API_BASE_URL=https://ваш-реальный-домен.com/api
```

Остальные Firebase параметры уже настроены!

---

## 📦 ЭТАП 4: СБОРКА APK

### Метод 1: Облачная сборка (РЕКОМЕНДУЕТСЯ)

Это самый простой способ - EAS соберёт APK на облачных серверах.

```bash
eas build --platform android --profile preview
```

**Процесс:**
1. EAS загрузит ваш код на облако
2. Соберёт APK (~10-20 минут)
3. Даст ссылку на скачивание

**Вывод будет примерно таким:**
```
✔ Build completed!
📦 Download APK: https://expo.dev/accounts/USERNAME/projects/keystone-app/builds/BUILD_ID
```

### Метод 2: Production APK

Для финальной версии:

```bash
eas build --platform android --profile production
```

---

## 📥 ЭТАП 5: СКАЧАТЬ APK

### Вариант A: Из терминала

После завершения сборки EAS выдаст прямую ссылку. Скопируйте её и откройте в браузере.

### Вариант B: Через веб-интерфейс

1. Откройте [expo.dev](https://expo.dev)
2. Войдите в аккаунт
3. Выберите проект "Keystone"
4. Перейдите в раздел "Builds"
5. Скачайте последний успешный APK

### Команда для просмотра всех сборок:

```bash
eas build:list
```

---

## 📱 ЭТАП 6: УСТАНОВКА НА ТЕЛЕФОН

### Способ 1: Через USB

1. Подключите Android телефон к компьютеру
2. Включите **USB-отладку** на телефоне:
   - Настройки → О телефоне → 7 раз нажмите "Номер сборки"
   - Настройки → Для разработчиков → USB-отладка (включить)
3. Скопируйте APK на телефон
4. Откройте файл APK на телефоне
5. Разрешите установку из неизвестных источников
6. Установите приложение

### Способ 2: Через QR-код

После сборки EAS создаёт QR-код:

```bash
eas build:view BUILD_ID
```

Отсканируйте QR-код телефоном и скачайте APK прямо на устройство.

### Способ 3: Через облако

1. Загрузите APK в Google Drive / Dropbox
2. Откройте ссылку на телефоне
3. Скачайте и установите

---

## 🧪 ЭТАП 7: ТЕСТИРОВАНИЕ

После установки проверьте:

- ✅ Приложение запускается
- ✅ Показывается экран авторизации
- ✅ API подключение работает
- ✅ Push уведомления приходят (если настроены в Firebase)
- ✅ Навигация между экранами работает

---

## 🛠️ ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ

### Просмотр статуса сборки

```bash
eas build:list
```

### Отменить сборку

```bash
eas build:cancel <BUILD_ID>
```

### Просмотреть логи сборки

```bash
eas build:view <BUILD_ID>
```

### Пересобрать без кэша

```bash
eas build --platform android --profile preview --clear-cache
```

### Локальная сборка (требуется Android Studio)

```bash
eas build --platform android --local
```

---

## 🔧 TROUBLESHOOTING

### Ошибка: "google-services.json not found"

✅ **Решено!** Файл уже создан в `firebase/google-services.json`

### Ошибка: "Package name mismatch"

Убедитесь, что в `app.json` указано:
```json
"package": "com.keystone_app"
```

### Ошибка: "EAS Project ID not found"

Запустите:
```bash
eas build:configure
```

### Сборка зависла

Отмените и перезапустите:
```bash
eas build:cancel <BUILD_ID>
eas build --platform android --profile preview --clear-cache
```

### APK установлен, но приложение не запускается

Проверьте `.env` файл - убедитесь, что API URL правильный.

---

## 📊 ЧЕК-ЛИСТ ПЕРЕД СБОРКОЙ

- [ ] Node.js 18+ установлен
- [ ] `npm install` выполнен успешно
- [ ] `eas-cli` установлен глобально
- [ ] `eas login` выполнен
- [ ] `eas build:configure` выполнен
- [ ] EAS Project ID добавлен в app.json
- [ ] `.env` файл обновлён с реальным API URL
- [ ] Firebase `google-services.json` присутствует
- [ ] Package name = `com.keystone_app`

---

## 🎯 БЫСТРЫЙ СТАРТ (ВСЁ В ОДНОЙ КОМАНДЕ)

Если всё уже настроено:

```bash
cd mobile-app && \
npm install && \
eas login && \
eas build:configure && \
eas build --platform android --profile preview
```

---

## 📞 ПОЛЕЗНЫЕ ССЫЛКИ

- [Expo Docs](https://docs.expo.dev/)
- [EAS Build Docs](https://docs.expo.dev/build/introduction/)
- [Firebase Console](https://console.firebase.google.com/)
- [Expo Dashboard](https://expo.dev/)

---

## ✅ РЕЗУЛЬТАТ

После выполнения всех шагов вы получите:

- ✅ Готовый APK файл
- ✅ Приложение с Firebase интеграцией
- ✅ Push уведомления настроены
- ✅ API подключение к backend
- ✅ Установленное приложение на Android телефоне

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Тестирование**: Проверьте все функции приложения
2. **Деплой backend**: Убедитесь, что API работает
3. **Firebase настройка**: Включите Cloud Messaging в Firebase Console
4. **Google Play**: Для публикации создайте AAB файл:
   ```bash
   eas build --platform android --profile production
   ```

---

**Время сборки**: ~15-20 минут (облачная сборка)  
**Размер APK**: ~40-60 MB

**Готово! 🎉**
