# Keystone Bot - Быстрая установка

## Установка одной командой

Для быстрой установки проекта выполните одну из команд:

### Вариант 1: Полная установка (рекомендуется)
```bash
curl -sSL https://raw.githubusercontent.com/keystone4tech-blip/bot-db-charm/main/install.sh | bash
```

### Вариант 2: Быстрая установка (требует предварительно установленные зависимости)
```bash
curl -sSL https://raw.githubusercontent.com/keystone4tech-blip/bot-db-charm/main/quick-install.sh | bash
```

## Требования

- Linux (Ubuntu/Debian/CentOS)
- Root или sudo доступ
- Установленные: git, docker, docker-compose

## Что делает установка

1. 📦 Клонирует проект из GitHub
2. 🔧 Устанавливает все зависимости
3. ⚙️ Настраивает .env файл
4. 🚀 Запускает все сервисы через Docker
5. 🔐 Настраивает SSL (опционально)

## После установки

- Проверить статус: `cd /home/deploy/keystone-bot && docker-compose ps`
- Проверить логи: `cd /home/deploy/keystone-bot && docker-compose logs -f`
- Остановить: `cd /home/deploy/keystone-bot && docker-compose down`

## Настройка

1. Отредактируйте `.env` файл с вашими настройками:
   ```bash
   nano /home/deploy/keystone-bot/.env
   ```

2. Перезапустите сервисы:
   ```bash
   cd /home/deploy/keystone-bot && docker-compose restart
   ```

## Обновление

Для обновления до последней версии:
```bash
cd /home/deploy/keystone-bot
git pull origin main
docker-compose down
docker-compose up -d
```

## Поддержка

Если возникли проблемы, обратитесь к полной документации: [DEPLOY.md](DEPLOY.md)